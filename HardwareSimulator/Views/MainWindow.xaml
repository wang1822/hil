<Window x:Class="HardwareSimulator.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:HardwareSimulator.ViewModels"
        xmlns:converters="clr-namespace:HardwareSimulator.Converters"
        mc:Ignorable="d"
        Title="硬件模拟数据平台 - Hardware Simulator" 
        Height="800" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <Window.Resources>
        <converters:StringToBrushConverter x:Key="StringToBrushConverter"/>
        <converters:SocToColorConverter x:Key="SocToColorConverter"/>
        <converters:TemperatureToColorConverter x:Key="TemperatureToColorConverter"/>
        <converters:IndexToBoolConverter x:Key="IndexToBoolConverter"/>
        <converters:IndexToVisibilityConverter x:Key="IndexToVisibilityConverter"/>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🔧" FontSize="28" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="硬件模拟数据平台" FontSize="24" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Hardware Data Simulation Platform" FontSize="12" Foreground="#BBDEFB"/>
                    </StackPanel>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="更新间隔:" Foreground="White" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox Width="80" VerticalAlignment="Center" Margin="0,0,15,0"
                              SelectedValue="{Binding UpdateInterval, Mode=TwoWay}"
                              SelectedValuePath="Tag">
                        <ComboBoxItem Content="500ms" Tag="500"/>
                        <ComboBoxItem Content="1000ms" Tag="1000" IsSelected="True"/>
                        <ComboBoxItem Content="2000ms" Tag="2000"/>
                        <ComboBoxItem Content="5000ms" Tag="5000"/>
                    </ComboBox>
                    
                    <Button Content="{Binding SimulationButtonText}" 
                            Command="{Binding ToggleSimulationCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Background="#FF5722" Margin="5,0"/>
                    
                    <Button Content="重置数据" 
                            Command="{Binding ResetDataCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Background="#607D8B" Margin="5,0"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Tab Navigation -->
            <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                <StackPanel Orientation="Horizontal" Margin="10,0,10,0">
                    <RadioButton Content="⚡ PCS 功率转换系统" 
                                 Style="{StaticResource TabButtonStyle}"
                                 IsChecked="{Binding SelectedModuleIndex, Converter={StaticResource IndexToBoolConverter}, ConverterParameter=0}"
                                 Command="{Binding SelectModuleCommand}" CommandParameter="0"
                                 GroupName="ModuleTabs"/>
                    <RadioButton Content="🔋 BMS 电池管理系统" 
                                 Style="{StaticResource TabButtonStyle}"
                                 IsChecked="{Binding SelectedModuleIndex, Converter={StaticResource IndexToBoolConverter}, ConverterParameter=1}"
                                 Command="{Binding SelectModuleCommand}" CommandParameter="1"
                                 GroupName="ModuleTabs"/>
                    <RadioButton Content="❄️ 空调系统" 
                                 Style="{StaticResource TabButtonStyle}"
                                 IsChecked="{Binding SelectedModuleIndex, Converter={StaticResource IndexToBoolConverter}, ConverterParameter=2}"
                                 Command="{Binding SelectModuleCommand}" CommandParameter="2"
                                 GroupName="ModuleTabs"/>
                </StackPanel>
            </Border>
            
            <!-- Module Panels -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="10">
                
                <!-- PCS Panel -->
                <Border Style="{StaticResource CardStyle}" 
                        Visibility="{Binding SelectedModuleIndex, Converter={StaticResource IndexToVisibilityConverter}, ConverterParameter=0}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="⚡ PCS 功率转换系统" Style="{StaticResource HeaderTextStyle}"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Ellipse Style="{StaticResource StatusIndicatorStyle}" 
                                         Fill="{Binding PcsViewModel.StatusColor, Converter={StaticResource StringToBrushConverter}}"/>
                                <TextBlock Text="{Binding PcsViewModel.StatusText}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Grid>
                        
                        <Separator Margin="0,5,0,10"/>
                        
                        <!-- DC Side -->
                        <TextBlock Text="直流侧 (DC Side)" FontWeight="SemiBold" Foreground="#1976D2" Margin="0,5,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="直流电压" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding PcsViewModel.Data.DcVoltage, StringFormat=F1}"/>
                                    <Run Text=" V" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="直流电流" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding PcsViewModel.Data.DcCurrent, StringFormat=F1}"/>
                                    <Run Text=" A" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="直流功率" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding PcsViewModel.Data.DcPower, StringFormat=F2}"/>
                                    <Run Text=" kW" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                        
                        <!-- AC Side -->
                        <TextBlock Text="交流侧 (AC Side)" FontWeight="SemiBold" Foreground="#1976D2" Margin="0,15,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="交流电压" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding PcsViewModel.Data.AcVoltage, StringFormat=F1}"/>
                                    <Run Text=" V" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="交流电流" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding PcsViewModel.Data.AcCurrent, StringFormat=F1}"/>
                                    <Run Text=" A" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="交流功率" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding PcsViewModel.Data.AcPower, StringFormat=F2}"/>
                                    <Run Text=" kW" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="频率" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding PcsViewModel.Data.Frequency, StringFormat=F2}"/>
                                    <Run Text=" Hz" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                        
                        <!-- Other Parameters -->
                        <TextBlock Text="其他参数" FontWeight="SemiBold" Foreground="#1976D2" Margin="0,15,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="功率因数" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding PcsViewModel.Data.PowerFactor, StringFormat=F3}"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="转换效率" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding PcsViewModel.Data.Efficiency, StringFormat=F1}"/>
                                    <Run Text=" %" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="设备温度" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}" 
                                           Foreground="{Binding PcsViewModel.Data.Temperature, Converter={StaticResource TemperatureToColorConverter}}">
                                    <Run Text="{Binding PcsViewModel.Data.Temperature, StringFormat=F1}"/>
                                    <Run Text=" °C"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="工作模式" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Text="{Binding PcsViewModel.WorkModeText}" Style="{StaticResource ValueStyle}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="运行时间" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding PcsViewModel.Data.RunningHours, StringFormat=F1}"/>
                                    <Run Text=" h" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                        
                        <!-- 参数设置区域 -->
                        <TextBlock Text="📝 参数设置 (下发至设备)" FontWeight="SemiBold" Foreground="#E65100" Margin="0,20,0,10"/>
                        <Border Background="#FFF3E0" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,5">
                                    <TextBlock Text="设定工作模式" Style="{StaticResource LabelStyle}"/>
                                    <ComboBox Width="150" HorizontalAlignment="Left" Margin="0,5,0,0"
                                              SelectedIndex="{Binding PcsViewModel.SetWorkModeIndex, Mode=TwoWay}">
                                        <ComboBoxItem Content="充电"/>
                                        <ComboBoxItem Content="放电"/>
                                        <ComboBoxItem Content="空闲"/>
                                        <ComboBoxItem Content="并网"/>
                                        <ComboBoxItem Content="离网"/>
                                    </ComboBox>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,5">
                                    <TextBlock Text="设定直流电压 (V)" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Width="150" HorizontalAlignment="Left" Margin="0,5,0,0"
                                             Text="{Binding PcsViewModel.SetDcVoltage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,5">
                                    <TextBlock Text="设定功率 (kW)" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Width="150" HorizontalAlignment="Left" Margin="0,5,0,0"
                                             Text="{Binding PcsViewModel.SetPower, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,15,0,0">
                                    <Button Content="📤 下发参数" Command="{Binding PcsViewModel.ApplySettingsCommand}"
                                            Style="{StaticResource PrimaryButtonStyle}" Background="#FF5722" Margin="0,0,10,0"/>
                                    <Button Content="📥 读取设备" Command="{Binding PcsViewModel.ReadFromDeviceCommand}"
                                            Style="{StaticResource PrimaryButtonStyle}" Background="#2196F3"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>
                
                <!-- BMS Panel -->
                <Border Style="{StaticResource CardStyle}" 
                        Visibility="{Binding SelectedModuleIndex, Converter={StaticResource IndexToVisibilityConverter}, ConverterParameter=1}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🔋 BMS 电池管理系统" Style="{StaticResource HeaderTextStyle}"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Ellipse Style="{StaticResource StatusIndicatorStyle}" 
                                         Fill="{Binding BmsViewModel.StatusColor, Converter={StaticResource StringToBrushConverter}}"/>
                                <TextBlock Text="{Binding BmsViewModel.StatusText}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Grid>
                        
                        <Separator Margin="0,5,0,10"/>
                        
                        <!-- SOC/SOH -->
                        <TextBlock Text="电池状态" FontWeight="SemiBold" Foreground="#388E3C" Margin="0,5,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" Margin="0,5">
                                <TextBlock Text="SOC 荷电状态" Style="{StaticResource LabelStyle}"/>
                                <ProgressBar Value="{Binding BmsViewModel.Data.Soc}" Maximum="100" Height="20" Margin="0,5,5,0"
                                             Foreground="{Binding BmsViewModel.Data.Soc, Converter={StaticResource SocToColorConverter}}"/>
                                <TextBlock Style="{StaticResource ValueStyle}" HorizontalAlignment="Center">
                                    <Run Text="{Binding BmsViewModel.Data.Soc, StringFormat=F1}"/>
                                    <Run Text=" %" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" Margin="0,5">
                                <TextBlock Text="SOH 健康状态" Style="{StaticResource LabelStyle}"/>
                                <ProgressBar Value="{Binding BmsViewModel.Data.Soh}" Maximum="100" Height="20" Margin="5,5,0,0"
                                             Foreground="#4CAF50"/>
                                <TextBlock Style="{StaticResource ValueStyle}" HorizontalAlignment="Center">
                                    <Run Text="{Binding BmsViewModel.Data.Soh, StringFormat=F1}"/>
                                    <Run Text=" %" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                        
                        <!-- Voltage -->
                        <TextBlock Text="电压信息" FontWeight="SemiBold" Foreground="#388E3C" Margin="0,15,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="总电压" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding BmsViewModel.Data.TotalVoltage, StringFormat=F1}"/>
                                    <Run Text=" V" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="总电流" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding BmsViewModel.Data.TotalCurrent, StringFormat=F1}"/>
                                    <Run Text=" A" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="最高单体电压" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding BmsViewModel.Data.MaxCellVoltage, StringFormat=F3}"/>
                                    <Run Text=" V" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="最低单体电压" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding BmsViewModel.Data.MinCellVoltage, StringFormat=F3}"/>
                                    <Run Text=" V" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="平均单体电压" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding BmsViewModel.Data.AvgCellVoltage, StringFormat=F3}"/>
                                    <Run Text=" V" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="单体压差" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding BmsViewModel.Data.CellVoltageDiff, StringFormat=F3}"/>
                                    <Run Text=" V" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                        
                        <!-- Temperature -->
                        <TextBlock Text="温度信息" FontWeight="SemiBold" Foreground="#388E3C" Margin="0,15,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="最高温度" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}" 
                                           Foreground="{Binding BmsViewModel.Data.MaxTemperature, Converter={StaticResource TemperatureToColorConverter}}">
                                    <Run Text="{Binding BmsViewModel.Data.MaxTemperature, StringFormat=F1}"/>
                                    <Run Text=" °C"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="最低温度" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}" 
                                           Foreground="{Binding BmsViewModel.Data.MinTemperature, Converter={StaticResource TemperatureToColorConverter}}">
                                    <Run Text="{Binding BmsViewModel.Data.MinTemperature, StringFormat=F1}"/>
                                    <Run Text=" °C"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="平均温度" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding BmsViewModel.Data.AvgTemperature, StringFormat=F1}"/>
                                    <Run Text=" °C" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="温差" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding BmsViewModel.Data.TemperatureDiff, StringFormat=F1}"/>
                                    <Run Text=" °C" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                        
                        <!-- Other -->
                        <TextBlock Text="其他信息" FontWeight="SemiBold" Foreground="#388E3C" Margin="0,15,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="可用能量" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding BmsViewModel.Data.AvailableEnergy, StringFormat=F1}"/>
                                    <Run Text=" kWh" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="绝缘电阻" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding BmsViewModel.Data.InsulationResistance, StringFormat=F0}"/>
                                    <Run Text=" MΩ" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="电池簇在线" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding BmsViewModel.Data.OnlineClusterCount}"/>
                                    <Run Text=" / "/>
                                    <Run Text="{Binding BmsViewModel.Data.ClusterCount}"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="告警等级" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Text="{Binding BmsViewModel.AlarmLevelText}" Style="{StaticResource ValueStyle}"
                                           Foreground="{Binding BmsViewModel.AlarmColor, Converter={StaticResource StringToBrushConverter}}"/>
                            </StackPanel>
                        </Grid>
                        
                        <!-- 参数设置区域 -->
                        <TextBlock Text="📝 参数设置 (下发至设备)" FontWeight="SemiBold" Foreground="#E65100" Margin="0,20,0,10"/>
                        <Border Background="#FFF3E0" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,5">
                                    <TextBlock Text="目标SOC (%)" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Width="150" HorizontalAlignment="Left" Margin="0,5,0,0"
                                             Text="{Binding BmsViewModel.SetTargetSoc, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,5">
                                    <TextBlock Text="充电电流限制 (A)" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Width="150" HorizontalAlignment="Left" Margin="0,5,0,0"
                                             Text="{Binding BmsViewModel.SetChargingCurrentLimit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,5">
                                    <TextBlock Text="放电电流限制 (A)" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Width="150" HorizontalAlignment="Left" Margin="0,5,0,0"
                                             Text="{Binding BmsViewModel.SetDischargingCurrentLimit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,15,0,0">
                                    <Button Content="📤 下发参数" Command="{Binding BmsViewModel.ApplySettingsCommand}"
                                            Style="{StaticResource PrimaryButtonStyle}" Background="#FF5722" Margin="0,0,10,0"/>
                                    <Button Content="📥 读取设备" Command="{Binding BmsViewModel.ReadFromDeviceCommand}"
                                            Style="{StaticResource PrimaryButtonStyle}" Background="#2196F3"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>
                
                <!-- AC Panel -->
                <Border Style="{StaticResource CardStyle}" 
                        Visibility="{Binding SelectedModuleIndex, Converter={StaticResource IndexToVisibilityConverter}, ConverterParameter=2}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="❄️ 空调系统" Style="{StaticResource HeaderTextStyle}"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Ellipse Style="{StaticResource StatusIndicatorStyle}" 
                                         Fill="{Binding AcViewModel.StatusColor, Converter={StaticResource StringToBrushConverter}}"/>
                                <TextBlock Text="{Binding AcViewModel.StatusText}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Grid>
                        
                        <Separator Margin="0,5,0,10"/>
                        
                        <!-- Temperature -->
                        <TextBlock Text="温度信息" FontWeight="SemiBold" Foreground="#00796B" Margin="0,5,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="室内温度" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}" 
                                           Foreground="{Binding AcViewModel.Data.IndoorTemperature, Converter={StaticResource TemperatureToColorConverter}}">
                                    <Run Text="{Binding AcViewModel.Data.IndoorTemperature, StringFormat=F1}"/>
                                    <Run Text=" °C"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="室外温度" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}" 
                                           Foreground="{Binding AcViewModel.Data.OutdoorTemperature, Converter={StaticResource TemperatureToColorConverter}}">
                                    <Run Text="{Binding AcViewModel.Data.OutdoorTemperature, StringFormat=F1}"/>
                                    <Run Text=" °C"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="设定温度" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding AcViewModel.Data.SetTemperature, StringFormat=F1}"/>
                                    <Run Text=" °C" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="运行模式" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Text="{Binding AcViewModel.ModeText}" Style="{StaticResource ValueStyle}"
                                           Foreground="{Binding AcViewModel.ModeColor, Converter={StaticResource StringToBrushConverter}}"/>
                            </StackPanel>
                        </Grid>
                        
                        <!-- Humidity -->
                        <TextBlock Text="湿度信息" FontWeight="SemiBold" Foreground="#00796B" Margin="0,15,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" Margin="0,5">
                                <TextBlock Text="室内湿度" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding AcViewModel.Data.IndoorHumidity, StringFormat=F1}"/>
                                    <Run Text=" %" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" Margin="0,5">
                                <TextBlock Text="室外湿度" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding AcViewModel.Data.OutdoorHumidity, StringFormat=F1}"/>
                                    <Run Text=" %" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                        
                        <!-- Compressor & Fan -->
                        <TextBlock Text="压缩机与风机" FontWeight="SemiBold" Foreground="#00796B" Margin="0,15,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="压缩机频率" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding AcViewModel.Data.CompressorFrequency, StringFormat=F1}"/>
                                    <Run Text=" Hz" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="风速档位" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Text="{Binding AcViewModel.FanSpeedText}" Style="{StaticResource ValueStyle}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="室内风机转速" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding AcViewModel.Data.IndoorFanSpeed}"/>
                                    <Run Text=" RPM" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="室外风机转速" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding AcViewModel.Data.OutdoorFanSpeed}"/>
                                    <Run Text=" RPM" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                        
                        <!-- Refrigerant -->
                        <TextBlock Text="制冷剂参数" FontWeight="SemiBold" Foreground="#00796B" Margin="0,15,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="蒸发器温度" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding AcViewModel.Data.EvaporatorTemperature, StringFormat=F1}"/>
                                    <Run Text=" °C" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="冷凝器温度" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding AcViewModel.Data.CondenserTemperature, StringFormat=F1}"/>
                                    <Run Text=" °C" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="吸气压力" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding AcViewModel.Data.SuctionPressure, StringFormat=F2}"/>
                                    <Run Text=" MPa" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="排气压力" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding AcViewModel.Data.DischargePressure, StringFormat=F2}"/>
                                    <Run Text=" MPa" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                        
                        <!-- Power -->
                        <TextBlock Text="能耗信息" FontWeight="SemiBold" Foreground="#00796B" Margin="0,15,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="实时功率" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding AcViewModel.Data.Power, StringFormat=F2}"/>
                                    <Run Text=" kW" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="累计能耗" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding AcViewModel.Data.TotalEnergyConsumption, StringFormat=F1}"/>
                                    <Run Text=" kWh" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,5">
                                <TextBlock Text="制冷量" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding AcViewModel.Data.CoolingCapacity, StringFormat=F2}"/>
                                    <Run Text=" kW" Foreground="#666"/>
                                </TextBlock>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,5">
                                <TextBlock Text="能效比 EER" Style="{StaticResource LabelStyle}"/>
                                <TextBlock Style="{StaticResource ValueStyle}">
                                    <Run Text="{Binding AcViewModel.Data.Eer, StringFormat=F2}"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                        
                        <!-- 参数设置区域 -->
                        <TextBlock Text="📝 参数设置 (下发至设备)" FontWeight="SemiBold" Foreground="#E65100" Margin="0,20,0,10"/>
                        <Border Background="#FFF3E0" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,5">
                                    <TextBlock Text="设定温度 (°C)" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Width="150" HorizontalAlignment="Left" Margin="0,5,0,0"
                                             Text="{Binding AcViewModel.NewSetTemperature, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,5">
                                    <TextBlock Text="运行模式" Style="{StaticResource LabelStyle}"/>
                                    <ComboBox Width="150" HorizontalAlignment="Left" Margin="0,5,0,0"
                                              SelectedIndex="{Binding AcViewModel.SetModeIndex, Mode=TwoWay}">
                                        <ComboBoxItem Content="制冷"/>
                                        <ComboBoxItem Content="制热"/>
                                        <ComboBoxItem Content="除湿"/>
                                        <ComboBoxItem Content="通风"/>
                                        <ComboBoxItem Content="自动"/>
                                    </ComboBox>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,5">
                                    <TextBlock Text="风速档位" Style="{StaticResource LabelStyle}"/>
                                    <ComboBox Width="150" HorizontalAlignment="Left" Margin="0,5,0,0"
                                              SelectedIndex="{Binding AcViewModel.SetFanSpeedIndex, Mode=TwoWay}">
                                        <ComboBoxItem Content="低速"/>
                                        <ComboBoxItem Content="中速"/>
                                        <ComboBoxItem Content="高速"/>
                                        <ComboBoxItem Content="自动"/>
                                    </ComboBox>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,5">
                                    <TextBlock Text="开关状态" Style="{StaticResource LabelStyle}"/>
                                    <CheckBox Content="开机" IsChecked="{Binding AcViewModel.SetPowerOn, Mode=TwoWay}" 
                                              Margin="0,5,0,0" VerticalContentAlignment="Center"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,15,0,0">
                                    <Button Content="📤 下发参数" Command="{Binding AcViewModel.ApplySettingsCommand}"
                                            Style="{StaticResource PrimaryButtonStyle}" Background="#FF5722" Margin="0,0,10,0"/>
                                    <Button Content="📥 读取设备" Command="{Binding AcViewModel.ReadFromDeviceCommand}"
                                            Style="{StaticResource PrimaryButtonStyle}" Background="#2196F3"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
        </Grid>
        
        <!-- Footer -->
        <Border Grid.Row="2" Background="#37474F" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="状态: " Foreground="#B0BEC5"/>
                    <TextBlock Foreground="White" FontWeight="SemiBold">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="已停止"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSimulating}" Value="True">
                                        <Setter Property="Text" Value="模拟运行中..."/>
                                        <Setter Property="Foreground" Value="#4CAF50"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Text="  |  更新次数: " Foreground="#B0BEC5"/>
                    <TextBlock Text="{Binding UpdateCount}" Foreground="White"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="最后更新: " Foreground="#B0BEC5"/>
                    <TextBlock Text="{Binding LastUpdateTime, StringFormat=HH:mm:ss}" Foreground="White"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
